import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Colors.indigo;
  static const Color background = Colors.black;
  static const Color white = Colors.white;
  static const Color grey = Colors.grey;

  // Status Colors
  static Color success = Colors.green.shade700;
  static Color error = Colors.redAccent.shade700;
  static Color warning = Colors.orangeAccent;

  // Overlay Colors
  static Color overlayGreen = Colors.green.withOpacity(0.8);
  static Color overlayRed = Colors.red.withOpacity(0.8);

  // Painter
  static const Color scannerColor = Colors.cyanAccent;
}